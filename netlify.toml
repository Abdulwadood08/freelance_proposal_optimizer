# Netlify build for monorepo — Next.js static export in frontend/
[build]
  base = "frontend"
  command = "npm run build"
  # Publish path is relative to the repository root (not the base directory).
  publish = "frontend/out"

[build.environment]
  NODE_VERSION = "20"
